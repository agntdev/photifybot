[
  {
    "name": "Upscale with no previous job shows prompt to generate",
    "steps": [
      {
        "send": { "callback": "action:upscale" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "No image to upscale. Tap 🎨 Generate to create one first."
            }
          }
        ]
      }
    ]
  }
]
