[
  {
    "name": "Variants generation sends photos",
    "steps": [
      {
        "send": { "callback": "option:variants" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "No prompt found. Tap /start to begin again."
            }
          }
        ]
      }
    ]
  }
]
