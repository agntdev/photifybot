[
  {
    "name": "Regenerate with no previous job shows prompt to generate",
    "steps": [
      {
        "send": { "callback": "action:regenerate" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "No previous generation to regenerate. Tap 🎨 Generate to create one."
            }
          }
        ]
      }
    ]
  }
]
